#include <bits/stdc++.h>
using namespace std;

int solve() {
	return 0;
}

int main() {
	//fast io
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	//constraints
	int T; cin >> T;

	while (T--) {
		int ans = solve();
		cout << ans << "\n";
	}

	return 0;
}
